UAG(MYSELF) {
    "$(user)"
}


HAG(MYHOST) {
   $(HOSTNAME) 
}

ASG(DEFAULT) {
    RULE(1,WRITE,TRAPWRITE)
}

ASG(SPECIAL) {
    RULE(1,WRITE,TRAPWRITE) {
        UAG(MYSELF)
        HAG(MYHOST)
    }
}

ASG(RO) {
    RULE(1, READ)
}
